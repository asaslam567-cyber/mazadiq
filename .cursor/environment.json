{
  "name": "mazad-alfadhli",
  "install": "sudo apt-get update && sudo apt-get install -y --no-install-recommends python3-venv && python3 -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip && pip install -r requirements.txt",
  "terminals": [
    {
      "name": "web",
      "command": ". .venv/bin/activate && PORT=5050 python serve.py"
    }
  ],
  "ports": [
    5050
  ]
}
